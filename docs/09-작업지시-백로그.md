# 09 · 작업 지시 백로그 (구현 세션용 핸드오프)

> **용도.** 전략 세션이 **결정**한 것 중 구현이 필요한 일을, **콜드 세션이 그대로 실행**할 수 있게 정리한 지시서.
> **실행 전 필독:** [00-전략-원칙.md](00-전략-원칙.md)(전략 SSOT) — 모든 작업은 이 원칙을 어기면 안 된다. 특히 **신뢰 4속성(§2)**·**안도-우선(원칙6)**·**부풀리기 금지(§5)**.
> **완료 규칙:** 작업 끝나면 typecheck + 전체 테스트 green 확인, 관련 문서(SSOT·07-계약·명세)의 해당 표시 갱신, 이 백로그에서 체크.
> **로컬 환경:** node 는 `.node/` 로컬. PowerShell에서 `$env:PATH="$PWD\.node;$env:PATH"` 후 `.node\node.exe .node\node_modules\npm\bin\npm-cli.js run typecheck | test:run`.

---

## T1 · 선행 오프셋 코드 제거 (✅ 완료 2026-09)

> **완료.** `OFFSET_MONTHS·OffsetMonths·PaceOffset·OffsetWarning·INITIAL_OFFSETS`·오프셋 적용 로직
> (`setPaceOffset·effectiveOffset·resolveTargetPeriod·assessOffsetRaise`)·`E-PACE-*`·`gate.ts` 제거.
> `pace.ts`=시기 산출만(`currentPublicGoals·cohortAlignedMonth·shiftYearMonth`). 스냅샷 `offsets` 필드 제거.
> SSOT §10-A 결정 A→완료, 07-계약 §1/§0 정리. 615 pass, tsc clean.
> (남은 07 stale: 죽은 GATE 절·StandardOrigin '오프셋 적용' 열 → **T3에서 정리**.)

**맥락:** SSOT §10-A 확정 — 선행(달력식 앞서기)은 제품에 없다. App 은 이미 오프셋 `[]`로 동작하나, **오프셋 기계장치가 코드·계약에 남아 문서·타입·동작 3자 불일치.** 이걸 닫는다.

**할 것 (제거):**
- `src/domain/types.ts` — `OFFSET_MONTHS`, `OffsetMonths`, `PaceOffset` 인터페이스.
- `src/domain/standards/child2021.ts` — `INITIAL_OFFSETS` (국어·수학 12).
- `src/domain/pace.ts` — 오프셋 적용 로직 전반(현재 시기 산출은 오프셋 없이 유지).
- `docs/07-계약.md` §1 (PaceOffset F3 · INV-PACE-01~04), §0 타입어휘의 `OffsetMonths`.
- 관련 테스트(오프셋 전제 테스트) 정리.

**하지 말 것 (경계):**
- 이건 **오프셋 제거지, 미래목표 접근 제거가 아니다.** `currentPublicGoals`가 "현재 시기 band만" 보여주는 동작은 **유지**한다.
- '다음 결'(§10-B)과 혼동 금지 — 그건 별개 기능(T4), 지금 만들지 않는다.

**수용 기준:**
- typecheck + 전체 테스트 green.
- 코드·화면·데이터에 '선행/오프셋' 개념 0.
- 완료 후 SSOT §10-A의 "코드 정리(팔로우업 · 미실행)" 문구를 "완료"로 갱신.

---

## T2 · 매핑 신뢰 4속성을 계약+테스트로 (활동→목표) — ✅ 완료 (2026-09)

> **완료.** ①프리셋 맵 `activityPresets.ts`(docs/10 인코딩, 영어 primary=[], `why`·`primaryElem` 포함) ·
> ②부풀리기 가드레일 `presetGuardrailViolations`(**band별** 1영역·≤2) · ③근거 `why` 필드(연결 시트 노출용) ·
> ④INV "매핑은 챙기는중만, 이룸 안 만듦" 테스트.
>
> **T2.3 배선 완료 — "A + B의 확인 단계" 혼합(docs/10 "매핑 확정 → 커버리지 배선").**
> - **영역-whole 자동커버 폐기.** `coverage.domainHasActivity`·`academy.attendanceActivities` 제거.
>   커버리지는 `publicGoalStatusOf` 가 활동 `targetIds` 로만 판정(등원 `coversDomains` 는 메타데이터·미사용).
> - **셋업 배선(§06-A).** 칩→프리셋(`presetByType`)→현재 band 필터(`suggestedTargets`)로 **primary 를 겨냥 목표로 확정.**
>   S2 학원 = 등원 엔티티 + **숙제 활동**(targetIds 확정) 자동 생성. S3 = 집 활동(targetIds 확정). 미리 체크→'시작하기'=확정(rule3).
>   칩↔프리셋은 **보수적 매핑**(결합칩='한글·독서'→대표 1개 '한글'; 프리셋 없는 칩=자동 겨냥 없음·과소청구). `SetupFlow` 칩에 `presetType`.
> - **관리 폼(§12).** 유형 프리셋도 영역 전체가 아닌 **프리셋 primary 만** 미리 체크(`ManageSheet.applyPreset`).
> - **band(Q3).** MVP=취학 전 `primary`(nuri-*)만. `primaryElem`(std-*) 슬롯만 두고 미큐레이션(현재 band 매칭 없으면 제안 안 함·원칙3).
> - **문서 갱신:** 07-계약 정정⑥-b·INV-ACAD-06 개정. 시드 픽스처(`boundary/seed.ts`)를 특정 목표 겨냥으로 갱신
>   (유아체육→예체능만 챙김·건강·안전은 정직하게 갭). 628 pass, tsc clean.

**맥락:** SSOT §2(신뢰 4속성)·§5(매핑 3필터·부풀리기 가드레일). 활동(예: 태권도)을 목표에 연결할 때 관련성으로 과잉매핑하면 커버리지가 부풀고 신뢰가 깨진다.

**할 것:**
1. **활동유형→후보목표 프리셋 맵** 신설(`src/domain/standards/` 아래). **큐레이션 원본 = [10-프리셋-활동목표-큐레이션.md](10-프리셋-활동목표-큐레이션.md) (전략 산출물 · 완료).** 그 표를 그대로 인코딩한다. 임의로 매핑을 지어내거나 추가하지 말 것. 데이터 형태·영어 예외·가드레일은 그 문서 하단 참고.
2. **부풀리기 탐지 테스트:** 한 활동의 매핑이 **목표 2개 초과** 또는 **영역 2개 이상**에 걸치면 테스트 실패(가드레일 자동화). (부수 opt-in 매핑은 별도 표기로 예외 처리.)
3. **근거 노출:** 매핑에 why/sourceRef 필드 → UI에서 확인 가능(§10-A 카드는 짧게, 상세는 연결 시트).
4. **INV 추가:** "매핑은 '챙기는 중'만 만든다. '이룸'은 만들지 않는다"(2축 불변식). 계약·테스트로.

**수용 기준:**
- 태권도 프리셋 = 예체능 신체활동(`nuri-phy-3`,`nuri-phy-4`) **1영역 2목표**. 인성·규칙은 부수 opt-in.
- 3영역/4목표 매핑 시 테스트가 실패로 잡는다.
- 부모 미확정 제안은 커버리지에 반영되지 않는다(신뢰 속성 ③).

---

## T6 · 초1~2 프리셋 `primaryElem` 채우기 — ⛔ T7로 흡수(스킵)

> **스킵.** T7(묶음 모델)이 프리셋 primary/primaryElem 을 **묶음 id**로 바꿔, 개별 std `primaryElem`(이 지시)은 불필요해졌다. T7 §"T6 관계" 대로 **T6 미실행**. 초1~2 매핑은 T7 프리셋의 `primaryElem`(묶음 id, 예 `cl-el-pe-body`)이 담당한다. 아래는 기록용.

**맥락:** 2018-01생(=초2) 온보딩 시 커버 0. 원인: `activityPresets.ts` 의 `primaryElem`(초1~2 std-*)이 전부 비어 있어, 초1~2 아이가 학원을 골라도 `suggestedTargets`가 현재 band(std-*)에서 매칭을 못 찾음 → targetIds=[] → 커버 0. (band 판정·배선은 정상. ACTIVITY_LIBRARY 와 무관.)

**할 것:** [10-프리셋-활동목표-큐레이션.md](10-프리셋-활동목표-큐레이션.md) "초1~2 primaryElem 큐레이션" 표를 `ACTIVITY_PRESETS` 각 항목의 `primaryElem` 에 그대로 인코딩:

| type | primaryElem |
|---|---|
| 태권도 · 유아체육 · 축구 · 수영 | `['std-2즐01-02']` |
| 발레 | `['std-2즐01-02','std-2즐04-03']` |
| 미술 | `['std-2즐04-03']` |
| 피아노 | `['std-2즐02-01']` |
| 한글 | `['std-2국04-01','std-2국02-01']` |
| 사고력수학 | `['std-2수02-01','std-2수03-03']` |
| 독서 | `['std-2국02-05','std-2국05-04']` |
| 영어 | (그대로 비움) |

**수용 기준:**
- `presetGuardrailViolations` 전 프리셋 통과(각 primaryElem ≤2·단일 영역=preset.domain). 위 값은 이미 만족하도록 큐레이션됨.
- 2018-01생 온보딩 + 태권도 선택 → 예체능 `std-2즐01-02` 챙기는중으로 뜬다. typecheck + 테스트 green.
- 임의로 목표 추가/변경 금지(docs/10 그대로). 한글은 챙기는중만(이룸 아님) 유지.

---

## T5 · 코호트 일반화 (생년월 기반) — ✅ 완료

> **완료.** 코호트 하드코딩(2021-01 단일) 제거. 앱은 **온보딩에서 입력받은 아이 나이(생년월)로 현재 시기를 산출**한다. 임의 나이(미취학~초2) 동작. 아래는 기록용.

**맥락:** SSOT §3 — ICP = 미취학~초2, 앱은 **아이 생년월로 현재 시기를 산출**한다.

**할 것:**
- 표준 데이터(`STANDARDS_2021` 등)에서 **코호트 상수 분리** — `CHILD_BIRTH_YM`/`SCHOOL_ENTRY_YM` 를 **입력(아이 프로필)** 으로. 커리큘럼 문장(누리 59 + 초1~2 100)은 코호트 무관 = 그대로.
- 생년월 → **현재 band 판정**(누리=취학 전 / 초1~2=std). 한국 입학연도는 출생연도+3월 컷오프로 결정적 함수.
- `currentPublicGoals` 가 임의 생년월에 대해 그 band 목표를 반환.
- 범위: **만 3세~초2**. 그 밖(0~2세 표준보육·초3+)은 "대상 아님" 처리(데이터 없음, 억지 X).
- 파일: `src/domain/standards/*`(child2021 → cohort-agnostic로 리네임/리팩터), `pace.ts`, 셋업(생년월 입력), 테스트.

**수용 기준:** 임의 생년월(예: 2022-05, 2019-09) 입력 시 올바른 band·현재 시기 목표가 나온다. typecheck + 테스트 green. 2021-01 케이스도 동일 결과 유지(회귀 없음).

**의존:** T2 배선(특정 목표 커버)·프리셋 band(primary/primaryElem)와 맞물림 — 함께 보는 게 좋다.

---

## T3 · 문서 정리 (진실원천 지도대로) — ✅ 완료 (2026-09)

> **완료.** SSOT §11 진실원천 지도대로 각 문서의 역할·stale 정리.
> - `04`·`05` → 상단에 코드/원문이 데이터 원천임 + SSOT(00) 포인터 + **선행/오프셋 폐기(§10-A·T1)** 배너. 04 §2·05 오프셋 항목에 폐기 표시.
> - `06-PRD`·`07-계약` → 상단 "전략은 00 참조" 추가. 07: 죽은 **§2-C OffsetGate 절**(INV-GATE-*·requiredForOffset·offsetGate) 제거(Achievement '됨' 개념만 남김), §0·§5-B의 **'오프셋 적용' 열** 삭제. 06: §4.4 PaceOffset·§4.4 폐기 표시.
> - `00~02 mom-test` → "2026-08 Discovery 산출물 · 현재상태 아님" 라벨.
> - `ux/프로토타입.html` → `docs/archive/프로토타입.html` 로 이동(git mv, 이력 보존). 00 §11·08 참조 갱신.
> - 범위 밖으로 남긴 것: 07 §3 `evaluateCoverage`(옛 5상태)·`해석`/B′ 서술은 선행 stale 아님 → 별도 문서 패스. 08 은 T3 대상 아님(경로 포인터만 갱신).
> - 전체 테스트 green(문서-코드 정합 `consistency.test.ts` 포함) 확인.

**맥락:** SSOT §11 진실원천 지도. 같은 사실이 여러 문서에 중복돼 드리프트 발생.

---

## T7 · 묶음(cluster) 모델 전환 + 나이별 온보딩 칩 — ✅ 완료 (2026-09)

> **완료.** 모델 **A**(활동이 묶음 id 를 겨냥). 구현 요약:
> - **묶음 데이터** `src/domain/standards/clusters.ts` — docs/11 §2 인코딩(취학전 14 + 초1~2 18). 무결성 테스트(`tests/domain/clusters.test.ts`).
> - **묶음=Standard 어댑터**: `clusterAsStandard`/`currentClusterStandards`/`currentClusterIds`/`bandOfMonth`. 묶음을 화면목표(Standard)로 표현해 coverage·today·guards 가 그대로 다룬다(리플 최소화). 개별 성취기준은 근거 상세로만 남음.
> - **화면목표 단위 전환**: App 이 `currentPublicGoals`(개별 std) → `currentClusterStandards`(묶음). 커버리지·배너·상세·오늘 모두 묶음 단위.
> - **프리셋 묶음화**: `activityPresets` primary/primaryElem = 묶음 id(docs/11 §4). 가드레일 = band별 ≤2묶음·1영역. 새 type(논술·연산·코딩·로봇·일기·글쓰기) 추가.
> - **온보딩 칩 band 분기**: `SetupFlow` 가 `bandOf(birthYm)` 로 미취학/초1~2 칩 세트 스위칭(docs/11 §3). presetType→묶음.
> - **상세 화면**: 묶음 카드 목록 + 각 묶음의 **근거 상세**(개별 성취기준, `<details>`). `MilestoneVM.evidence`.
> - **이룸 optional**: 커버리지 기본 = 챙기는중/비어있음. 됨은 부모 표시(achieved)만.
> - **시드/테스트**: `boundary/seed.ts` targetIds 를 묶음 id 로. 관련 테스트 전부 묶음 기준으로 갱신. 639 pass, tsc clean. 초2(2018-01) 온보딩 브라우저 검증(초등 칩·묶음 커버리지).
> - 개별 성취기준 STANDARDS_2021 은 **삭제하지 않고** 근거로 유지(docs/11 §6.7).
> - 미완(팔로우업): 추천(ACTIVITY_LIBRARY)의 묶음 롤업(§6.5)은 미구현 — 현재 묶음 갭엔 추천 미표시(원칙3 위반 아님). 별도 과제.

**맥락:** 초1~2 성취기준 100개가 너무 많고·복잡하고·이룸 판정 불가. 부모 단위를 **교육과정 묶음**으로 올린다. 안도는 **사교육 기반**("네가 시키는 게 이만큼 챙김"). 큐레이션 원본 = [11-묶음-온보딩-큐레이션.md](11-묶음-온보딩-큐레이션.md) (전략 산출물 · 완료). **그대로 인코딩, 임의 변경 금지.**

**할 것:**
1. **묶음 데이터**(`standards/clusters.ts`): doc11 §2 표 인코딩(id·영역·member std, child2021 참조). 취학전 14 + 초1~2 18.
2. **화면목표 단위 전환:** `currentPublicGoals` → 현재 band **묶음** 반환. 커버리지·상태·안도카드·커버리지바 모두 묶음 단위.
3. **프리셋 묶음화:** `activityPresets` primary/primaryElem = 개별 std → **묶음 id**(doc11 §4). 가드레일 = **band별 ≤2묶음·1영역**. (→ T6의 개별-std primaryElem 방식을 **대체**.)
4. **온보딩 칩 band 분기**(`SetupFlow`): doc11 §3 두 세트(유아용/초등용)를 현재 band로 스위칭. presetType→묶음.
5. **이룸 optional:** 부모가 묶음 이룸을 강제로 판정하지 않게. 기본 챙기는중/비어있음.
6. **추천 롤업:** 갭=묶음이 되므로 ACTIVITY_LIBRARY 추천을 묶음 대표 목표에 매칭.
7. 개별 성취기준은 **삭제 금지** — 묶음 근거 상세로 유지.

**수용 기준:**
- 2018-01생(초2) 온보딩 → 국어 6묶음 등 **묶음 단위**로 뜨고, 초등용 칩(영어학원·수학학원…)이 보인다. 고른 학원이 해당 묶음을 챙기는 중으로.
- 만5세 온보딩 → 유아용 칩 + 취학전 14묶음. 회귀 없음.
- 가드레일·전체 테스트 green. 문서-코드 정합 테스트 갱신.

> **T6 관계:** T6(초1~2 primaryElem 개별 std 채우기)는 T7로 흡수된다 — T7 하면 프리셋이 묶음 id를 쓰므로 개별 std primaryElem은 불필요. **T7을 하면 T6 스킵.**

---

## T8 · 두 레인 + 등원/숙제 구분 — ✅ 완료 (2026-09)

> **완료.** 구현 요약:
> - **두 레인**(`src/domain/lanes.ts`): 생활·마음 = 사회·인성+건강·안전 / 학습(활동) = 나머지(국·영·수·과·**예체능**). 예체능은 사교육 활발 → 학습 레인(전략 결정).
> - **AreaScreen**: 학습 레인(gap-우선 그룹) + **생활·마음 안심 섹션**(calm 카드, "일상에서 챙겨지고 있어요", 갭 알람 X). 개요 카운트는 학습 레인만.
> - **배너(안도 카드)**: 학습 레인만 gap/on 집계 — 생활·마음 빈칸은 배너 갭에서 제외(SSOT §5).
> - **등원/숙제 구분**: `ActivityPreset.coverMode`(등원형/숙제형 기본값). `Academy.coversClusters`(등원 커버 묶음). `academy.attendanceActivities` 복원 — 등원형 학원의 등원이 특정 묶음을 커버(오늘 화면엔 일정만, 체크 X). 셋업 S2가 coverMode로 분기(등원형=학원+coversClusters·숙제 없음 / 숙제형=학원+숙제 활동).
> - 시드: 미술·유아체육=등원형(coversClusters), 더하다=숙제형. 648 pass, tsc clean. 브라우저 검증(태권도 등원 커버·한글 숙제 체크·생활 안심 섹션).

**맥락:** SSOT §5 "두 레인" + "학원 커버 방식". 사회·인성·건강·안전은 지우지 않되 **안심 레인**으로, 학원 커버는 **등원/숙제 정직 구분**.

**할 것:**
1. **두 레인 UI.** 영역/현황을 **학습(국·영·수·과)** 과 **생활·마음(사회·인성·건강·안전)** 으로 구분 표시.
   - 학습 레인: 활동·추천·커버리지·학원 pull(기존).
   - 생활·마음 레인: **저액션·안심.** 프레이밍 "유치원·일상에서 챙겨지고 있어요". **빈칸을 갭 알람으로 띄우지 않는다**(허니 넛지 대신 안심 톤). 커버리지 바 계산·안도 카드에서 학습 레인과 분리 집계 권장.
2. **등원/숙제 구분.** `Academy`(또는 활동)에 커버 방식: `등원형` | `숙제형` | `둘다`.
   - **등원형:** 등원 자체가 프리셋 묶음을 커버(별도 숙제 활동 생성 X). 오늘 화면 = 등원 일정만(체크 X). 라벨 "○○ 다니면서 챙겨져요".
   - **숙제형:** 숙제 활동이 묶음 커버. 오늘 화면 = 숙제 체크 항목. 라벨 "○○ 숙제로 챙겨져요".
   - **기본값 = 활동유형별**(태권도·수영·미술·체육·발레·피아노=등원형 / 한글·수학·논술·학습지=숙제형). 셋업/관리에 **"숙제 있어요?" 토글**.
   - T2가 만든 "모든 학원=숙제 활동 자동생성"을 위 구분으로 교체. (원래 등원=일정·숙제=활동 모델 복원.)

**수용 기준:**
- 태권도 등록 → "다니면서 챙겨져요", 오늘 화면에 숙제 체크 항목 **안** 생김(등원 일정만). 한글학원 → "숙제로 챙겨져요" + 오늘 숙제 체크.
- 생활·마음 레인이 빈칸이어도 불안 톤이 아님. 학습 레인과 시각적으로 구분.
- 전체 테스트 green. 문서-코드 정합 갱신.

---

## T9 · 온보딩 재구조화(과목별) + 생활 관찰 체크 모듈 — ✅ 완료 (2026-09)

> **완료.** 두 입력 모드(SSOT §5 · 명세 §06-A/06-B):
> - **① 과목별 온보딩(§06-A)**: `SetupFlow` S2 = 과목 카드(국·영·수·과·예체능). 각 카드 학원 칩 + "숙제 있어요?" 토글(coverMode) + 집 활동 칩. 온보딩은 학습 레인만. 예체능 카드 포함(전략 결정). 2스텝.
> - **② 생활 관찰 체크(§06-B)**: `observationChecks.ts`(doc11 §7 질문, 취학전10/초1~28) + `ObservationCheck.tsx` 모듈. 예/아직/모름, 모든 질문 '예'→묶음 이룸(보수적). 진입점=영역 화면 생활·마음 섹션("3분 우리 애 체크"). 결과 안도 톤, 학습 레인과 분리. "테스트"·점수 표현 금지(OOS-6).
> - 668 pass, tsc clean. 브라우저 검증(과목 카드·숙제 토글 / 관찰 체크→사회·인성 "이미 1곳 확인").

**맥락:** SSOT §5 "두 입력 모드". 자유 칩 대신 **학습=과목별 입력**, **생활=별도 관찰 체크**. T8(두 레인·등원/숙제)과 맞물림.

**할 것:**
1. **학습 레인 온보딩 = 과목별 카드.** 국어·영어·수학·과학 각각: `다니는 학원?` → (있으면) `숙제 있나요?` 토글 + `집에서 하는 활동?`(칩/직접). 과목당 한 카드로 압축, **스킵 가능**("지금 하는 것만"). 등원형/숙제형 구분(T8)·프리셋 묶음(T7) 그대로 물림. 기존 자유 칩(SUBJECTS/HOME) 대체.
   - 톤: "어디 다녀요?"(현재 하는 것 기록), 추천 목록 아님(SSOT §9).
2. **생활 관찰 체크 모듈**(온보딩 밖 · 재방문 훅): "3분 우리 애 체크". 질문 = [11-묶음-온보딩-큐레이션.md](11-묶음-온보딩-큐레이션.md) §7 그대로(취학전 10 / 초1~2 8, band별). **예/아직/모름** 3택. "예"=이룸(achieved) 기록. "아직"=넛지 없음. "모름"=미표시.
   - ⚠️ **"테스트" 표현 금지**(OOS-6). "체크"·"우리 애 요즘 어때요?". 결과 화면 = 안도 톤("생활·마음도 이만큼 챙겨지고 있어요"), 학습 레인과 분리 집계.
   - 홈/현황에 "생활·마음 체크해보기" 진입점(반복 가능).

**수용 기준:**
- 온보딩: 학습 4과목 카드로 입력, 각 과목 학원/숙제/집활동이 해당 묶음 커버로 반영. 생활 질문은 온보딩에 **없음**(별도 모듈).
- 생활 체크 완료 → 해당 묶음 이룸 표시 + 안도 톤. 미완/모름이 갭 알람으로 안 뜸.
- 전체 테스트 green. 문서-코드 정합 갱신.

---

## T10 · §07-A 안도·공유 카드 + 등원/숙제 라벨 (체크리스트 F·D) — ✅ 완료 (2026-09)

> **완료.** 12-초2-통과기준-체크리스트 검증에서 걸린 **F(안도-우선)·D(라벨)** 수정.
> - **공유 카드 좌표 코드 제거**(명세 §07-A L313 "N5·3C7 류 표기 안 함"). `shareCode` 삭제.
> - **공유·안도 카운트를 학습 레인 묶음 기준으로**(생활 분리). ShareSheet "N묶음 중 M묶음". App `learnVMs`/`shareTotal`.
> - **안도 배너 사교육 톤**: "○○·○○로 N묶음 챙기고 있어요"(근거=학원·활동 이름, 학교 배지 없음). `GapBanner.sources`·`onClusters`. 조사 로/으로 헬퍼.
> - **등원/숙제 라벨**(D): 상세 챙김 문구 = 등원형 "○○ 다니면서 챙겨져요" / 숙제형 "○○ 숙제로 챙겨져요". `MilestoneVM.coverKind`. `등원로`→`다니면서`로 조사 버그 해소.
> - 669 pass, tsc clean. 초2 브라우저 검증(배너·공유·상세 라벨). 체크리스트 A~H 전 항목 통과.

---

## T4 · '다음 결' (완료 시 opt-in 미리보기) — 보류(MVP 낮음)

**맥락:** SSOT §10-B. 현재 시기를 다 이룬 영역의 "그다음"을 선행 없이 보여준다. **컨시어지 이후.**

**만들 때 조건(5개 전부):** ①그 영역 실제 전부 이룸일 때만 ②갭·커버리지·넛지와 무관 ③연속성·준비도 프레이밍("억지로 말고") ④영역별로만 ⑤오프셋 아님(데이터는 다음 band `std-*`를 커버리지 바깥 미리보기로).

**지금은 착수 금지.** 우선순위 낮음 표시.

---

## 대기 중 · 전략 세션(나) 산출물 필요
- ~~T2 프리셋 큐레이션 표~~ → **완료: [10-프리셋-활동목표-큐레이션.md](10-프리셋-활동목표-큐레이션.md).** T2 실행 가능.
- **ACTIVITY_LIBRARY 배지 승격** — i-누리 놀이사례 원문 확보 시 해당 self 항목을 nuri 로(가짜 인용 금지). 원문 확보는 전략 세션에서 판단.

---

*작업 지시 백로그 · 전략 결정은 [00-전략-원칙.md], 이력은 git.*
