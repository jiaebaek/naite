# 09 · 작업 지시 백로그 (구현 세션용 핸드오프)

> **용도.** 전략 세션이 **결정**한 것 중 구현이 필요한 일을, 콜드 세션이 그대로 실행할 수 있게.
> **실행 전 필독:** [00-전략-원칙.md](00-전략-원칙.md)(SSOT). 특히 §2 신뢰·§4 원칙·§5-B 성장좌표·§12 구현규율.
> **완료 규칙:** 끝나면 typecheck + 전체 테스트 green, 관련 문서(00·명세) 갱신, 여기 체크. 동작은 계약(불변식)+테스트로 못박음(SSOT §12).
> **환경:** node=`.node/` 로컬. PowerShell `$env:PATH="$PWD\.node;$env:PATH"` 후 `.node\node.exe .node\node_modules\npm\bin\npm-cli.js run typecheck | test:run`.

---

## ✅ 완료 이력 (2026-09 · 코드 반영됨)
- **선행/오프셋 제거** — 달력식 선행 개념·코드 전부 제거(OFFSET*/PaceOffset/gate 등). 시기=입력 나이 기반.
- **코호트 일반화** — 하드코딩(2021-01) 제거, 온보딩 생년월로 현재 시기 산출(미취학~초2).
- **묶음(cluster) 모델 + 나이별 온보딩 칩** — 개별 성취기준(100)→묶음(초1~2 18/취학전 14). 화면목표=묶음.
- **매핑 신뢰 4속성 + 가드레일** — 활동유형 프리셋(활동→묶음, band별 ≤2·1영역), 부풀리기 테스트, "매핑=챙김만·이룸 아님".
- **첫 문 통찰-먼저** — 입력-먼저→통찰-먼저(생년월+유치원). (명세 §6·§7)
- **문서 대청소** — 옛 기준 문서 삭제, SSOT/명세 재정합(2026-09).

---

## 🔜 활성 (지금 할 것)

### A · 성장 좌표 구현 (★ 핵심·큼)
> **진행(2026-09):** ✅ 도메인 레이어(취학전 14 GrowthPoint + 결정적 좌표 계산 + 가드레일 테스트) · ✅ §9 영역 상세 = GrowthPoint 프로필(🟢강점/🟡다음경험·근거 뒤) · ✅ §10 영역별 관찰 체크(behavior 예/아직/모름 → 좌표 order↑). **남음:** 영역 탭 조망 연결(B와 함께)·초1~2 큐레이션(전략). 브라우저 검증 완료.
**맥락:** SSOT §5-B. "기준과 아이 상태가 섞임"이 최대 병 → 4층 분리. 데이터·과학탐구 풀예시 = [14-성장좌표-데이터모델-과학탐구.md](14-성장좌표-데이터모델-과학탐구.md).
- 타입: `GrowthPoint`(묶음 기반)·`ObservableBehavior`·`Evidence`·`GrowthState`(계산)·`NextExperience`.
- **좌표 계산:** Evidence(부모관찰+활동+등원+이룸)→최고 order→**부모 서술**(내부 ordinal 비노출).
- **영역 상세 = GrowthPoint 프로필**(명세 §9): 🟢강점 먼저·🟡다음경험, "고유 모양" 캡션, 근거는 탭 뒤.
- **관찰 체크**를 전 영역으로(§10, 예/아직/모름). 문항 = [11 §7](11-묶음-온보딩-큐레이션.md).
- 가드레일(SSOT §5-B): 숫자/단계 부모 비노출 · 또래비교·도달선 금지 · 덜 보임=다음경험 · 겸손 톤.
- **기존 묶음/활동/커버리지 위에 상태 레이어 얹기(갈아엎기 X).**
- ⚠️ 관찰행동·다음경험 **콘텐츠 큐레이션 = 전략 세션 산출물**(임의 자작 금지). **취학전 전 영역 준비 완료**: 과학탐구 [14](14-성장좌표-데이터모델-과학탐구.md) + 나머지 6영역 [15](15-성장좌표-큐레이션-취학전.md). 이 둘을 그대로 인코딩. **초1~2(std-*)는 아직 미큐레이션** → 취학전부터 구현.

### B · 영역 탭 lean (명세 §8)
잘한 건 접힌 한 줄, 살펴볼 것 1~2개만. pip·바·3그룹·pill 남발 제거(상태는 말로). 생활 레인·3분 체크 유지. 목업 `ux/mockup-영역탭-단순화.html`. **표시만 바꿈**(계산 불변). ※ A(성장좌표)와 함께 보면 좋음.

### C · 재방문 델타 (명세 §11)
기록/재진입 시 "지난번보다 달라졌어요"(새 모습·이룸·월령) → 다음 경험. = 리텐션 방아쇠(SSOT §8).

### D · UI 카피 "공교육"→"전문가가 세운 기준"
`vm.ts` recommendVM/badgeOf 등 UI 문자열의 "공교육·…" → 표준 이름(`누리과정`/`성취기준`) 또는 "전문가가 세운 기준". 내부 `origin='공교육'` 등 데이터·enum은 불변. UI grep 전수. 문서-코드 정합 테스트 기대값 갱신.

---

## ⏱ 로드맵 (코어 검증 후)
- **비용 렌즈(학원 견적 통합)** — SSOT §12. 비용↔커버리지 연결, ⛔상위%·랭킹 금지, Pull·무료병기. 챙김 검증 후 fast-follow. 학원 DB만 병행 준비.
- **'다음 결'**(완료 시 opt-in 미리보기) — SSOT §10-B. 보류(MVP 낮음).

## 대기 · 전략 세션(나) 산출물
- ✅ **취학전 성장좌표 콘텐츠 큐레이션** 완료 — [14](14-성장좌표-데이터모델-과학탐구.md)(과학탐구) + [15](15-성장좌표-큐레이션-취학전.md)(국·수·사회인성·예체능·건강안전).
- **초1~2(std-*) 성장좌표 큐레이션** — 취학전 구현·검증 후 같은 포맷으로.
- ✅ **앱 지인 테스트 계획**(가설·게이트) 완료 — [16](16-앱-지인테스트-계획.md). 취학전 구현 나오면 실행.
- **교사/전문가 자문 영입 판단** — 권위 카피를 사람-권위로 승격하는 전제(SSOT §2·doc13 D4). H3(신뢰) 재실패 시 트리거.

---

*작업 지시 백로그 · 전략 [00](00-전략-원칙.md) · 이력 git.*
